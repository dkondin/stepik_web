error_log /home/box/web/nginx.error_log

server {
    listen       80;

    location ^~ /uploads/ {
        root   /home/box/web/;
    }

    location ~* ^.+\.\w+$ {
        root   /home/box/web/public/;
    }

    location / {
        return 404;
        }
}


